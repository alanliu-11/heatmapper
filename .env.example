BRIGHTDATA_USER=
BRIGHTDATA_PASS=
BRIGHTDATA_API_TOKEN=
SECRET_KEY=

# VAPID keys for Web Push (generate with: python -c "from py_vapid import Vapid; v=Vapid(); v.generate_keys(); print('private:', v.private_pem()); print('public:', v.public_key)")
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=

# Email digest (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@heatmapper.local
